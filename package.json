{
  "name": "mon-portfolio",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install:app": "npm --prefix Projet_React ci",
    "dev": "npm --prefix Projet_React run dev --",
    "build": "npm --prefix Projet_React run build",
    "preview": "npm --prefix Projet_React run preview",
    "lint": "npm --prefix Projet_React run lint",
    "format": "npm --prefix Projet_React run format"
  }
}
